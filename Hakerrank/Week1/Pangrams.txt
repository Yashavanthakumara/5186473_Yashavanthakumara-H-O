#include<stdio.h>
#include<ctype.h>

int main(){
    char s[1000];
    int alf[26]={0};
    int count=0;
    
    scanf("%[^\n]",s);
    for(int i=0; s[i] !='\0'; i++)
    {
        char chr =tolower(s[i]);
        if(chr>='a' && chr<='z'){
            int i =chr-'a';
            if(alf[i]==0)
            {
                alf[i]=1;
                count++;
            }
        }
        
    }
    if(count==26){
        printf("pangram");
    }
    else {
    printf("not pangram");
    }
    return 0;
}
